Drop screenshots here, referenced from the root `README.md`:

- `dashboard.png` — main Dashboard page
- `docker.png` — Docker containers page
- `vms.png` — Virtual machines page
- `offline.png` — the friendly error state shown when the Unraid server is unreachable

Recommended: 720px wide, light mode, actual data (blur/redact hostnames or IPs you don't want public).
